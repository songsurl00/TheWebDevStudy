// Array
const colors = ['red', 'orange', 'yellow', 4];

// 배열은 문자열 처럼 index화 되어있음
colors[0]; // "red"

colors.length; // 4

// Array methods:
array.push(value); // value를 배열 끝에 추가
array.pop(); // 배열 끝에 있는 값을 반환하고 삭제
array.unshift(value); // value를 배열 앞에 추가
array.shift(); // 배열 앞에 있는 값을 반환하고 삭제